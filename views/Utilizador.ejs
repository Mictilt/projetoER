<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>UMa Farmácia</title>
    <%- include('./partials/Style')%>
    <%- include('./partials/Style_Utili')%>
</head>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.0/jquery.min.js"></script>
<body>
<%- include('./partials/Base_Logged')%>
<% 
let numTicket = 0;
let nomeAtual = "";
let emailAtual = "";
console.log(tickets.User);
tickets.forEach(function(tickets) {
    if (tickets.User != null) {
    if (tickets.state==true && tickets.User._id == user.id) {
        numTicket++;
    } 
    if (tickets.User._id == user.id) {
        nomeAtual = tickets.User.username
        emailAtual = tickets.User.email;
    }
}
});
%>
<main class="utilizador flex-fill">
    <div  class="uti_detalhes">
       <div style=" color: black;">
            <div >
                <h2>Meus Detalhes:</h2>
            </div>
            <div style="display: inline-flex; color: black;">
                <h4 >Nome:</h4><span><%=udocs.username%></span><br>
            </div>
            <div style=" color: black;">
                <div style="display: inline-flex; color: black;">
                    <h4>Tipo:</h4><span>
                        <%if(udocs.tipo == 0){%>Inativo<%}%>
                        <%if(udocs.tipo == 1){%>Habitante local<%}%>
                        <%if(udocs.tipo == 2){%>Visitante<%}%>
                        <%if(udocs.tipo == 3){%>Motorista<%}%>
                        <%if(udocs.tipo == 4){%>Admin<%}%>                
                    </span><br>
                </div>
                    
            </div>
            <div style="display: inline-flex; color: black;">
                <h4>Email:</h4><span><%=udocs.email%></span><br>    
            </div>
        </div>
       
        
        
        
    </div>
    <div class="introdRes">
        <h2>Alterar palavra passe:</h2>
        <form  action = "/user/edit/<%=udocs._id%>" method="post">
            <input type="text" id="password" name="password" placeholder="nova palavra passe"> 
            <input type="hidden" name="username" value="<%=udocs.username%>">
            <input type="hidden" name="email" value="<%=udocs.email%>">            
        <button>Alterar</button>
            </form>
    </div>
   
</main>
<%-include('./partials/Footer')%>
</body>
</html>